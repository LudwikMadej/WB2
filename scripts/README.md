# scripts

## sync_data.sh

Synchronizacja katalogu `data/` z Cloudflare R2 przez rclone. Szczegóły użycia w głównym README.

## migrate_csv_to_parquet.py

Jednorazowa migracja aktywacji z CSV do parquet (float16, zstd).
